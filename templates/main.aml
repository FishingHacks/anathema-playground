if showing == "Editor"
    @editor (run->run_aml, error->editor_error)
else if showing == "Error"
    @error { errors: showing.errors }
else
    zstack
        @tempfile
        hstack
            spacer
            text  [bold: true] "Press Ctrl+E to get back to the editor  "